// @flow
import {observable, computed} from "mobx";
import moment from "moment";
import type {Task, Company} from "../Model";
import { Firebase, MapboxClient } from "../components";

export default class ListsDetailStore {
  
}
